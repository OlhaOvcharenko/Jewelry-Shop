/*SELECTORS*/

/* ACTIONS */
const reducerName = 'catalogue/products';
const createActionName = name => `app/${reducerName}/${name}`;

/* ACTIONS CREATORS */


/* REDUCER */
const productsReducer = (statePart = [], action) => {
  switch (action.type) {
    default:
  return statePart;
  }
}